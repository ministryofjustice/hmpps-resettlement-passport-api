server:
  shutdown: immediate

aws:
  provider: localstack

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

api:
  base:
    url:
      oauth: http://localhost:8090
      prison-register: http://localhost:8093
      offender-search: http://localhost:8094
      cvl: http://localhost:8095
      community: http://localhost:8096
      arn: http://localhost:8097
      prison: http://localhost:8098


spring:
  devtools:
    add-properties: true

  flyway:
    url: jdbc:postgresql://localhost:5432/resettlement-passport?sslmode=prefer
    user: resettlement-passport
    password: resettlement-passport

  datasource:
    url: 'jdbc:postgresql://localhost:5432/resettlement-passport?sslmode=prefer'
    username: resettlement-passport
    password: resettlement-passport

feature:
  incentives-reference-data-source-of-truth: true